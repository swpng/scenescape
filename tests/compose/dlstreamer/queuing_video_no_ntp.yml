# SPDX-FileCopyrightText: (C) 2025 Intel Corporation
# SPDX-License-Identifier: Apache-2.0
networks:
  scenescape-test:

secrets:
  root-cert:
    file: ${SECRETSDIR}/certs/scenescape-ca.pem

services:
  queuing-video:
    image: docker.io/intel/dlstreamer-pipeline-server:2025.2-ubuntu24-rc2
    privileged: true
    networks:
      scenescape-test:
    tty: true
    entrypoint: ["./run.sh"]
    ports:
      - "8081:8080"
      - "8555:8554"
    devices:
      - "/dev/dri:/dev/dri"
    depends_on:
      broker:
        condition: service_started
      queuing-cams:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-I", "-s", "http://localhost:8080/pipelines"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    environment:
      - RUN_MODE=EVA
      - DETECTION_DEVICE=CPU
      - CLASSIFICATION_DEVICE=CPU
      - ENABLE_RTSP=true
      - RTSP_PORT=8554
      - REST_SERVER_PORT=8080
      - GENICAM=Balluff
      - GST_DEBUG=1,gencamsrc:2
      - ADD_UTCTIME_TO_METADATA=true
      - APPEND_PIPELINE_NAME_TO_PUBLISHER_TOPIC=false
      - MQTT_HOST=broker.scenescape.intel.com
      - MQTT_PORT=1883
    configs:
      - source: queuing-config
        target: /home/pipeline-server/config.json
    volumes:
      - ./:/workspace
      - ./dlstreamer-pipeline-server/user_scripts:/home/pipeline-server/user_scripts
      - vol-dlstreamer-pipeline-server-pipeline-root:/var/cache/pipeline_root:uid=1999,gid=1999
      - vol-sample-data:/home/pipeline-server/videos
      - vol-models:/home/pipeline-server/models
    secrets:
      - source: root-cert
        target: certs/scenescape-ca.pem

configs:
  queuing-config:
    file: ./dlstreamer-pipeline-server/queuing-config-no-ntp.json

volumes:
  vol-models:
  vol-sample-data:
  vol-dlstreamer-pipeline-server-pipeline-root:
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs
